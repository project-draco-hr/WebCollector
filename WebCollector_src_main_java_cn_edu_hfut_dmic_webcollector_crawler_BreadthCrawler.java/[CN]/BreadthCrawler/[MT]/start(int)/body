{
  if (!resumable) {
    if (seeds.size() == 0) {
      Log.Infos("error:" + "Please add at least one seed");
      return;
    }
    if (regexs.size() == 0) {
      autoRegex();
    }
  }
  inject();
  initGenerator();
  for (int i=0; i < depth; i++) {
    Log.Infos("info","starting depth " + (i + 1));
    if (generator != null)     generate();
 else     break;
  }
}
