{
  DemoSogouCrawler crawler=new DemoSogouCrawler();
  for (int i=1; i <= 5; i++) {
    crawler.addSeed("http://www.sogou.com/web?query=" + URLEncoder.encode("??????") + "&page="+ i);
  }
  crawler.setThreads(50);
  crawler.start(2);
}
