{
  if (!crawler.couldStart()) {
    output("???????????????????????????URL!");
    return;
  }
  String localpath=txt_path.getText().trim();
  crawler.setRoot(localpath);
  try {
    threads=Integer.valueOf(txt_threads.getText());
  }
 catch (  Exception e) {
  }
  crawler.start(threads);
  output("????????????????????????" + localpath + "???????????????????????????,????????????????????????");
}
