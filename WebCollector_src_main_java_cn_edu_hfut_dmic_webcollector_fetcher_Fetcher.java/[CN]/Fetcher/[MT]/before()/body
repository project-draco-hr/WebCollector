{
  try {
    if (dbManager.isLocked()) {
      dbManager.merge();
      dbManager.unlock();
    }
  }
 catch (  Exception ex) {
    LOG.info("Exception",ex);
  }
  dbManager.lock();
  dbManager.initSegmentWriter();
  running=true;
}
